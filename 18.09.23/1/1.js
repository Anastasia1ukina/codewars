function printerError(s) {
    let str = ''
    let strLength = s.length
      if (s.match(/[a-m]/gi)) {
          str = `0/${s.length}`
          return str 
       } else {
         return s.every(n => )
       }
  }